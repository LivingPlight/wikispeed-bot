Install Node.js from nodejs.org

Open a terminal and run:git clone https://github.com/LivingPlight/wikispeed-bot
cd wikispeed-bot
npm install

Create your own .env file with your own bot token and channel ID

Run node index.js

run git pull to get the latest version
